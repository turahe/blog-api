name: Swagger Docs

on:
  push:
    branches: [main, "release/*"]
  pull_request:
    paths:
      - "main.go"
      - "docs/**"
      - "internal/adapters/inbound/**"
      - "Makefile"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  swagger:
    name: Generate + route smoke
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # make swagger / routes-check run swag and go test inside Docker (see Makefile).
      - name: Regenerate docs
        run: make swagger

      - name: Ensure docs committed
        run: |
          git diff --exit-code -- docs/ || {
            echo "docs/ is stale — run 'make swagger' and commit"
            exit 1
          }

      - name: Route registration smoke tests
        run: make routes-check
